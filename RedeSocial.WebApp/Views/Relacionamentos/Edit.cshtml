@model RedeSocial.Domain.Entities.Relacionamento

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Amizade</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PerfilIdA" />
            <div class="form-group">
                <label asp-for="PerfilIdA" class="control-label"></label>
                <select asp-for="PerfilIdA" class="form-control" asp-items="ViewBag.PerfilIdA"></select>
            </div>
            <input type="hidden" asp-for="PerfilIdB" />
            <div class="form-group">
                <label asp-for="PerfilIdB" class="control-label"></label>
                <select asp-for="PerfilIdB" class="form-control" asp-items="ViewBag.PerfilIdB"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
